{
  "name": "paperboy-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prebuild": "cd paperboy && npm install --include=dev",
    "build": "cd paperboy && npm run build",
    "start": "cd paperboy && npx serve dist",
    "dev": "cd paperboy && npm run dev"
  }
}
